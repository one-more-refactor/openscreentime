[package]
name = "sentinel-policy"
version = "0.2.0"
edition = "2021"
rust-version = "1.89"
description = "Sentinel — the shared Policy document (stored by the server, edited by the web, enforced by the agent)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
